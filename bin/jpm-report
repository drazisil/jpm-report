#!/usr/bin/env node

var jpm_report = require('../src/index.js')

jpm_report.checkArgs(process.argv, function cb_parse_args (err, res) {
  if (err) {
    console.log(err)
  } else {
    console.log(res)
  }
})
