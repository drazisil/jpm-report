machine:
  node:
    version: 0.12.0
  environment:
    COVERALLS_REPO_TOKEN: "8HyRfjr1bW9lZjJOQ7WE3RaGf9y4Gmlz4"
    CIRCLE_ARTIFACTS: "~/jpm-report/coverage"

general:
  artifacts:
    - "coverage" # relative to the user's home directory

dependencies:
  override:
    - npm install
    
test:
  post:
    - "bin/jpm-report --version"
    - "bin/jpm-report test.txt"
